services:
    web:
        image: 'include image URL'
        container_name: web
        ports:
            - '80:80'
        volumes:
            - ./:/app
            - /app/node_modules
        environment:
            - DEBUG=1
